1. mkdir ApiDev
   cd ApiDev
   git init
   git config --global user.name "Prahlad10Bhat"
   git config --global user.email "prahlad10bhat@gmail.com"
   echo "bin/" >> .gitignore
   echo "obj/" >> .gitignore
   git remote add origin https://github.com/Prahlad10Bhat/ApiDev
   git add .
   git commit -m "Initial commit with .gitignore setup"
   git push -u origin master

2. git checkout -b api-feature
   echo "console.log('API Loaded');" > api.js
   git add api.js
   git commit -m "Add initial API functionality"
   echo '{"settings": {"version": "1.0"}}' > config.json
   git add config.json
   git commit -m "Add API configuration file"
   git push -u origin api-feature
   
3. a) Go to your GitHub repository in a web browser.
   b) You should see a "Compare & pull request" button at the top of the page if you've just pushed your branch. Click that button.
   c) In the "base" dropdown, select the main branch (this is the branch you want to merge into).
   d) In the "compare" dropdown, select the api-feature branch (the branch you're merging from).
   e) Add a title and a description for your pull request.
   f) Click the Create pull request button.Edit the file, save and push the changes.

4. git checkout api-feature
   mkdir -p .github/workflows
   touch .github/workflows/ci.yml
   git add .github/workflows/ci.yml
   git commit -m "Add GitHub Actions CI pipeline to api-feature branch"
   git push origin api-feature
  
5. git checkout main
   git pull origin main  
   git merge api-feature  
   git tag v2.0 
   git tag 
   git push origin v2.0  
   git ls-remote --tags origin  
   git checkout v1.5 
   git log --oneline  
   git push origin main --force  







