1. mkdir SampleProject
   cd SampleProject
   git init
   git config --global user.name "Prahlad10Bhat"
   git config --global user.email "prahlad10bhat@gmail.com"
   echo "# SampleProject" > README.md
   git add README.md
   git commit -m "Initial commit with README"
   git remote add origin https://github.com/Prahlad10Bhat/sample-project.git
   git branch -M main
   git push -u origin main

2. git checkout main
   git checkout -b feature-enhancement
   echo "console.log('Feature enhancement update');" > app.js
   git add app.js
   git commit -m "Updated app.js with feature enhancement"
   git checkout main
   git merge feature-enhancement
   git push origin main
   
3. git checkout main
   git log --oneline --graph --decorate -n 5
   git revert HEAD
   git push origin main
