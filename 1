# Create and switch to the development branch
git checkout -b development
touch init.txt
git add init.txt
git commit -m "Initial commit on development branch"

# Create and switch to feature-x branch
git checkout -b feature-x
touch init.txt
git add init.txt
git commit -m "Initial commit on feature-x branch"
